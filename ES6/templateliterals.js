// template literals

function printData(name, age) {
    let toPrint = `Student's name is ${name}
  and age is ${age}`;
    console.log(toPrint);
  }
  
  let sName = "Daya";
  let sAge = 23;
  printData(sName, sAge);